name: Deploy to Netlify

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy site to Netlify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install netlify-cli
        run: npm install -g netlify-cli

      - name: Deploy to Netlify (production)
        env:NETLIFY_AUTH_TOKEN
     NETLIFY_AUTH_TOKEN: ${{ secrets.nfp_XWC1zLx9ApD42dYj8jHVdpM3v5Sx6Y4X05d7 }}
        NETLIFY_SITE_ID: ${{ secrets.cbb95a1a-ed28-4a80-bfed-c3fa683daa2a }}
        run: |
          # Deploy the repository root as a static site
          netlify deploy --dir=. --prod --auth "$NETLIFY_AUTH_TOKEN" --site "$NETLIFY_SITE_ID"

# Note: Set the GitHub repository secrets `NETLIFY_AUTH_TOKEN` and `NETLIFY_SITE_ID`.
